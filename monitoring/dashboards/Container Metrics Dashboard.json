{
  "annotations": { "list": [] },
  "editable": true,
  "id": null,
  "links": [],
  "panels": [
    {
      "type": "timeseries",
      "title": "CPU Usage per Container",
      "targets": [
        { "expr": "rate(container_cpu_usage_seconds_total[1m])", "legendFormat": "{{name}}", "refId": "A" }
      ],
      "gridPos": { "x": 0, "y": 0, "w": 24, "h": 10 }
    },
    {
      "type": "timeseries",
      "title": "Memory Usage per Container",
      "targets": [
        { "expr": "container_memory_usage_bytes", "legendFormat": "{{name}}", "refId": "B" }
      ],
      "gridPos": { "x": 0, "y": 10, "w": 24, "h": 10 }
    },
    {
      "type": "timeseries",
      "title": "Network Usage per Container",
      "targets": [
        { "expr": "rate(container_network_receive_bytes_total[1m])", "legendFormat": "{{name}} RX", "refId": "C" },
        { "expr": "rate(container_network_transmit_bytes_total[1m])", "legendFormat": "{{name}} TX", "refId": "D" }
      ],
      "gridPos": { "x": 0, "y": 20, "w": 24, "h": 10 }
    },
    {
      "type": "timeseries",
      "title": "Disk I/O per Container",
      "targets": [
        { "expr": "rate(container_fs_reads_bytes_total[1m])", "legendFormat": "{{name}} Read", "refId": "E" },
        { "expr": "rate(container_fs_writes_bytes_total[1m])", "legendFormat": "{{name}} Write", "refId": "F" }
      ],
      "gridPos": { "x": 0, "y": 30, "w": 24, "h": 10 }
    }
  ],
  "schemaVersion": 36,
  "style": "dark",
  "tags": ["cadvisor", "container", "system"],
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "browser",
  "title": "Container Metrics Dashboard",
  "version": 2
}